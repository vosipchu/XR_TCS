# # config file version
apiVersion: 1

providers:
 - name: 'Server-Telegraf'
   orgId: 1
   folder: 'Server Details'
   type: file
   editable: true
   disableDeletion: false
   options:
     datasource: InfluxDB-Telegraf
#     prefix: InfluxDB
     path: /var/lib/grafana/dashboards/server-telegraf
