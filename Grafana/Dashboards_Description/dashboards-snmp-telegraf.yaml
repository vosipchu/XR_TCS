# # config file version
apiVersion: 1

providers:
 - name: 'SNMP-Telegraf'
   orgId: 1
   folder: 'SNMP (Telegraf)'
   type: file
   editable: true
   disableDeletion: false
   options:
     datasource: InfluxDB-Telegraf
#     prefix: InfluxDB
     path: /var/lib/grafana/dashboards/snmp-telegraf
